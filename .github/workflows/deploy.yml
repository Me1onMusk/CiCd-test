
name: GitHub Actions 실행하기

on:
  push:
    branches:
      - main

jobs:
  My-First-CiCd-Job:
    runs-on: ubuntu-latest
    steps:
      - name: simple-web
        run: echo "This is the first cicd step"

      - name: Github Variables 사용하기
        run:
          echo $GITHUB_SHA
          echo $GITHUB_Repository

      - name: Github Secret Variables 사용하기
        run: |
          echo ${{ vars.MY_ENV_NAME }}
          echo ${{ vars.MY_ENV_SECRET }}
