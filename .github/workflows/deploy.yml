
name: GitHub Actions 실행하기

on:
  push:
    branches:
      - main

jobs:
  My-First-CiCd-Job:
    runs-on: ubuntu-latest
    steps:
      - name: simple-web
        run: echo "This is the first cicd step"
